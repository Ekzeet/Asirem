// src/lib/checkout.ts — stub; real implementation lands in Task 7
export async function startCheckout(_opts: { courseId: string; email?: string; coupon?: string }): Promise<void> {
  alert('Checkout is wired in Task 7')
}
